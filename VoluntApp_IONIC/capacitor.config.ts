import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'VoluntApp_IONIC',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
